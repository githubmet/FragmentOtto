<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hikmetanil.fragmentotto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".P001FragmentAppCompat"/>
        <activity android:name=".P002AddRemoveFragment"/>
        <activity android:name=".P003FragmentLifecycle"/>
        <activity android:name=".P004AddFragmentXml"/>
        <activity android:name=".P005FragmentCommunication"/>
        <activity android:name=".P006FragComFaceToFace"/>
    </application>


</manifest>